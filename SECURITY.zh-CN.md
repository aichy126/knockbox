# 安全策略

[English](SECURITY.md) · **简体中文**

## 报告漏洞

发邮件到 **info@miramiao.com**。安全问题请不要开公开 issue。

写清楚你做了什么、发生了什么、你预期的是什么。有复现步骤更好，但不是必须的。

这是一个人维护的项目，七天内会收到第一次回复。确认有效的问题会在下一个版本修复，
公告里会署上你的名字，除非你不希望这样。

只维护最新一个版本，不向更早的 tag 回补。

## 仓库里的那把 APNs 密钥

`internal/library/apns/embedded/AuthKey_TXYBPWPD2L.p8` 是**一把真实可用的 Apple
推送密钥，刻意提交进仓库的**。部署之前请读完这一节。

### 为什么放在这里

APNs 的 device token 绑定在 bundle id 上，只有该 app 所属 Team 的密钥推得动它。
没有一把共享密钥的话，自建 Knockbox 就必须先有一个每年 99 美元的 Apple 开发者账号，
并且自己构建 iOS 客户端。这把密钥的作用，是让你跑自己的服务器、同时仍然用
App Store 上那个客户端。

### 它能做什么、不能做什么

这是一把 **topic-specific** 密钥，Apple 从 2025 年 2 月起支持这种类型。
与 Team 级密钥不同，它绑定在单个 bundle id 上。

- 它能给 `com.miramiao.knockbox` 推通知，也就是 App Store 上的 Knockbox 客户端。
- 它**推不了**任何别的 app，包括同一个开发者账号下的其它 app。
- 它**推不了**沙箱环境。沙箱是另一把密钥，不在这个仓库里，
  所以这把公开密钥碰不到任何人用 Xcode Debug 构建出来的客户端。
- 它**读不到**任何东西。APNs 是单向发送的。

### 风险如实说

给一台设备推消息需要两样东西：这把密钥，和那台设备的 APNs token。
密钥是公开的，所以剩下的防线全部落在 token 上。

APNs token 是 Apple 发给某台设备上某个 app 的一个不透明值。它在密码学意义上不算秘密，
但也不会出现在任何公开的地方：它只存在于设备上，以及它配对的那台服务器的
`device.apns_token` 列里，**明文**。攻击者要拿的就是这一列。
也就是说，**你的数据库被拖走，等于这个实例上所有设备都能被推任意通知**，
而且我们这边轮换密钥也挡不住——请按这个前提对待数据库文件。

即便如此他仍然做不到的事：读你的消息、进你的服务器、影响任何别的 app。
APNs 是单向发送的，而这把密钥只绑定一个 bundle id。

相对把密钥捏在自己手里，这确实是一次安全性上的降级。它换来的是「不需要开发者账号」，
这笔交易值不值得，由你自己判断。

### 怎样不再依赖它

把服务端指向你自己的密钥和你自己的 bundle id：

```toml
[apns]
topic = "com.example.yourbuild"

[apns.production]
key_id   = "YOURKEYID"
key_file = "/etc/knockbox/AuthKey_YOURKEYID.p8"
```

之后你需要自己构建并签名 iOS 客户端，因为 App Store 版的 bundle id 是
`com.miramiao.knockbox`。

### 轮换

如果这把密钥被滥用，被 Apple 限流或吊销，它会被换掉。换新密钥意味着
**每一个自建实例都必须升级才能继续收到推送**——内置的密钥没有办法远程更新。

轮换会同时在两个地方公告：

- 本仓库的 GitHub Security Advisory；
- 携带新密钥那个版本的 release notes。

想收到通知，在本仓库点 **Watch → Custom → Releases and Security advisories**。
如果你在为别人运行 Knockbox，请一定订阅。

## 服务端持有什么

下面这些是设计取舍，不是缺陷。README 的取舍那一节讲得更细，这里的简版是给做威胁建模用的。

- **不做端到端加密。** 服务端明文存储消息正文，因为它还要生成推送摘要、提供历史。
  安全边界就是你跑它的那台机器。
- **频道 token 明文存库**，这样 app 才能给你一条可以直接粘贴的 `curl`。
  它是只写凭据、只作用于一个频道，按 token 限流，随时可以轮换或删除。
- **设备凭据以 SHA-256 哈希存储。** 指的是 app 用来读历史、放在
  `Authorization: Bearer` 里的那个 token，明文只在配对的那一刻返回一次。
- **APNs device token 明文存储。** 只能如此——每次推送都要把它原样发给 Apple。
  它和一把公开密钥放在一起意味着什么，见上一节。
- **管理员密码用 bcrypt 哈希。** 会话存在服务端、可以随时吊销；改密码会让所有已有会话失效。
- **附件链接带 HMAC 签名并会过期。** 签名密钥在首次启动时随机生成并存进数据库，
  没有默认值。
- **配对码是 40 位**，一次性、有效期短、按 IP 限流。这么短的码能让人手输而不出问题，
  靠的是这三条一起生效。
- **清空是真的清空。** SQLite 开着 `secure_delete`，清空之后还会做一次 WAL checkpoint
  和增量 vacuum。它不能保证的是字节真的离开了 SSD——磨损均衡在应用层之下。

## 部署建议

- 用 HTTPS，并把 `server.external_url` 设成 `https://` 开头的地址。
  配对码、频道 token、附件签名链接都走它。
- 除非确有需要，不要把管理界面暴露到公网。按设计，它能读到这个实例上所有人的消息。
- 密钥类配置走环境变量而不是配置文件，例如 `IGO_APNS_PRODUCTION_KEY_BASE64`。
- 不打算让陌生人注册的话，保持 `public.enabled = false`。

## 不受理的范围

- 只有扫描器输出、没有实际影响演示的报告。
- 内置的那把 APNs 密钥本身。上面已经写清楚了，它是刻意的选择；
  「仓库里有一把私钥」这样的报告不提供任何新信息。
- 报告者自己配置出来的加固缺失，比如把管理界面用明文 HTTP 挂在公网地址上。
